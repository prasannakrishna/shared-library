include this jar and add this in properties
spring:
  application:
    name: order-service

service:
  core:
    logging:
      enabled: true

spring.zipkin.base-url: http://zipkin-server:9411
spring.sleuth.sampler.probability: 1.0
to pick log